enum AuthenticationStatus { byDefault, successed, failed, pending }
enum AuthenticationMethod {connection, registration}
//TODO: maybe add a "InProcess" value during the async operation and display a loader.
